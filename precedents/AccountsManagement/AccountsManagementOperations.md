# Описание Системных Операций

## 1. Операция: `loadData(userID: UserID)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными.

### **Предусловия**
- Пользователь аутентифицирован в системе и имеет активную сессию.
- База данных доступна для чтения информации учетной записи.
- Пользователь инициировал действие для загрузки информации учетной записи.

### **Постусловия**
- Текущие данные учетной записи загружены и отображены пользователю.
- Данные подготовлены для возможного редактирования.

### **Действия**
1. Система вызывает метод `UserAccount::loadData(userID)`.
2. Информация учетной записи, включая контактные и платежные данные, загружается из базы данных.
3. Данные отображаются пользователю для редактирования.

---

## 2. Операция: `validateData(data: UserData)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Редактирование личных данных).

### **Предусловия**
- Пользователь внес изменения в данные учетной записи и инициировал их сохранение.
- Система настроена на проверку данных по форматам, включая контактную информацию и платежные данные.

### **Постусловия**
- Введенные данные проверены на соответствие формату, допустимым значениям и правилам системы.
- При обнаружении ошибок система уведомляет пользователя.

### **Действия**
1. Система вызывает метод `UserAccount::validateData(data)`.
2. Проверяются формат данных, такие как телефон, email и адрес.
3. При ошибках возвращается сообщение, подсвечиваются некорректные поля.

---

## 3. Операция: `saveChanges(userID: UserID, data: UserData)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Редактирование личных данных).

### **Предусловия**
- Данные учетной записи успешно прошли проверку на корректность.
- Пользователь подтвердил изменения.

### **Постусловия**
- Обновленные данные учетной записи сохранены.
- Пользователь получает подтверждение завершения операции.

### **Действия**
1. Система вызывает метод `UserAccount::saveChanges(userID, data)`.
2. Данные сохраняются в базе.
3. Пользователь получает уведомление.

---

## 4. Операция: `updateDetails(userID: UserID, paymentInfo: PaymentInfo)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Изменение платежной информации).

### **Предусловия**
- Пользователь инициировал изменение платежной информации.
- Платёжная система доступна для обновления данных.

### **Постусловия**
- Платёжная информация пользователя обновлена и сохранена.
- Пользователь уведомлен об успешном обновлении.

### **Действия**
1. Система вызывает метод `Payment::updateDetails(userID, paymentInfo)`.
2. Данные обновляются и сохраняются.
3. Пользователь получает уведомление об успешной операции.

---

## 5. Операция: `confirmDeletion(userID: UserID)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Удаление учетной записи).

### **Предусловия**
- Пользователь инициировал удаление учетной записи.
- Система предоставляет предупреждение о последствиях.

### **Постусловия**
- Подтверждение удаления получено.
- Система готова завершить процесс удаления.

### **Действия**
1. Система вызывает метод `UserAccount::confirmDeletion(userID)`.
2. Пользователю отправляется уведомление о последствиях.
3. Система ожидает подтверждения.

---

## 6. Операция: `delete(userID: UserID)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Удаление учетной записи).

### **Предусловия**
- Пользователь подтвердил удаление учетной записи.

### **Постусловия**
- Учетная запись пользователя удалена.
- Лог удаления сохранен для аудита.

### **Действия**
1. Система вызывает метод `UserAccount::delete(userID)`.
2. Данные пользователя удаляются из базы.
3. Система логирует факт удаления.

---

## 7. Операция: `notifyUser(userID: UserID, message: String)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (Уведомление о действиях).

### **Предусловия**
- Событие, требующее уведомления, завершено.
- Каналы связи активны.

### **Постусловия**
- Пользователь уведомлен об изменениях.

### **Действия**
1. Система вызывает метод `NotificationService::notifyUser(userID, message)`.
2. Уведомление отправляется на e-mail или SMS.
3. Лог уведомлений сохраняется.

---

## 8. Операция: `checkInactivity()`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (расширение: Проверка активности).

### **Предусловия**
- Настроена периодичность проверки.
- Система работает стабильно.

### **Постусловия**
- Система проанализировала активность учетных записей.
- Уведомления отправлены администраторам и пользователям.

### **Действия**
1. Система вызывает метод `Admin::checkInactivity()`.
2. Пользователи и администраторы получают уведомления.

---

## 9. Операция: `restoreSession(userID: UserID)`

**Ссылки:**  
Прецеденты: Управление учетной записью и данными (расширение: Восстановление после сбоя).

### **Предусловия**
- Система была перезапущена.
- Пользователь обратился за восстановлением доступа.

### **Постусловия**
- Пользователь возвращен к последнему действию перед сбоем.

### **Действия**
1. Администратор перезапускает систему.
2. Вызывается метод `UserAccount::restoreSession(userID)`.
3. Пользователь продолжает работу с системой.

---
